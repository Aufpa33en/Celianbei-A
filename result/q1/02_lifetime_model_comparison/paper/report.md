# Q1 单调寿命外推模型族比较

状态：支持性生成记录；权威副本已接入 `result/q1/raw` 与 `result/q1/paper`。

嵌套留一电池比较选择 `linear`，策略等权 151—200 RMSE 为 0.000706，最坏电池 RMSE 为 0.002271。

三模型族 T80 包络的电池级最大跨度比中位数为 2.76，最大值为 4.34。该跨度是模型形式敏感性，不是置信区间。
