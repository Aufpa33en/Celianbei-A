# 设计审计

`design_audit.csv`每行对应一个参数完整策略标签，记录输入`C1、Q1、C2`以及推导得到的`T0、J、H、J_high_50/60/70`。

主要用途：

- 核实8个标签只对应7个唯一参数坐标；
- 区分7个非基准等`T0`标签和6个明确的新结构标签；
- 比较理想恒流时间`T0`与实验中的实际平均充电时间；
- 为留一唯一坐标验证提供分组标识。

输入来自第一问清洗数据，只读生成。
